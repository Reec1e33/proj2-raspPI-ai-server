Flask==2.2.5
numpy==1.23.5
# FastAPI alternative: "fastapi" and "uvicorn"
# If you use TFLite inside the backend, you might also specify:
# tflite-runtime  # but typically installed outside or in a venv
